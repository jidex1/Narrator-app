rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow read/write only for the authenticated owner at:
    // /users/{userId}/audio/{files...}
    match /users/{userId}/audio/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
